Java收到举报 → POST /api/moderate → Python
                                    │
                                    ▼
                         ┌─ classify_node ─┐
                         │ LLM判断内容类型   │
                         │ 输出: type,reason │
                         └───────┬─────────┘
                                 │
                          type == "normal" ?
                          ├─ 是 → 跳到 action_node（score=0，直接放行）
                          └─ 否 ↓
                         ┌─ retrieve_node ─┐
                         │ RAG检索相关法规  │  复用已有的retriever
                         │ 输出: rag_context│
                         └───────┬─────────┘
                                 │
                                 ▼
                         ┌─ judge_node ────┐
                         │ LLM判定违规      │
                         │ 输出: score,     │
                         │  violated_rules, │
                         │  violated_laws   │
                         └───────┬─────────┘
                                 │
                          score >= 0.6 ?
                          ├─ 否 → 跳到 action_node（无需证据）
                          └─ 是 ↓
                         ┌─ evidence_node ─┐
                         │ LLM生成证据报告   │
                         │ 输出: evidence   │
                         └───────┬─────────┘
                                 │
                                 ▼
                         ┌─ action_node ───┐
                         │ 纯逻辑，不调LLM  │
                         │ score→action映射 │
                         └───────┬─────────┘
                                 │
                                 ▼
                         返回结果给Java